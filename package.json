{
  "name": "@kint/bruno_shared_scripts",
  "version": "1.0.0",
  "exports": "./es/index.js",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild ./src/index.js --bundle --format=cjs --outdir=es --external:axios"
  },
  "dependencies": {
  },
  "devDependencies": {
    "esbuild": "0.23.0"
  }
}
